const { contracts: setupContracts } = require('./setup');
const { ethers } = require('hardhat');

describe('placeOrder', function () {
  const contracts = setupContracts();

  it('runs', async function () {
  });
});
